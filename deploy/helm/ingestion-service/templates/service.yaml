apiVersion: v1
kind: Service
metadata:
  name: {{ include "cats-common.fullname" . }}
  labels:
    {{- include "cats-common.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cats-common.selectorLabels" . | nindent 4 }}
